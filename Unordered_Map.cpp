/* The difference b/w map and unordered map is about this:
1. Inbuilt implementation 
2. Time Complexity
3. valid keys datatype

-> Unordered map don't use tree for the implementation It uses Hash table
-> It don't store value in sorted order
-> In this Insertion and accessing of element takes O(1) time.

*/

#include <bits/stdc++.h>
using namespace std;

int main(){
    unordered_map<int,string> m;
    //All other every thing will be similar as map like insertion and functions
    return 0;
}